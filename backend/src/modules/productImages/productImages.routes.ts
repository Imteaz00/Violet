// import { Router } from "express";
// import * as productImageControler from "./productImages.controller.js";
// import { requireAuth } from "@clerk/express";

// const productImageRouters = Router();

// // productImageRouters.get("/", productImageControler.getAllProductImage);
// productImageRouters.post(
//   "/create",
//   requireAuth(),
//   productImageControler.createProductImage
// );
// // productImageRouters.get("/:id", productImageControler.getProductImageById);
// // productImageRouters.delete("/:id", requireAuth(), productImageControler.deleteProductImage);

// export default productImageRouters;
